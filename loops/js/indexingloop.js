var arr = ["apple", "banana", "mango", "orange", "strawberry"];



for (var index = 0; index < arr.length; index++) {
    document.write(arr[index] + "<br>");
}

for (var index = 0; index < arr.length; index++) {
    document.write("Element at index " + index + " is " + arr[index] + "<br>");
}